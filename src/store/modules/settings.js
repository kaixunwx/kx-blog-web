import defaultSettings from '@/settings'
const { defaultThumbnail, footerTxt, caseNumber } = defaultSettings


const settings = {
    state: {
        defaultThumbnail: defaultThumbnail,
        footerTxt: footerTxt,
        caseNumber: caseNumber
    }
}

export default settings
